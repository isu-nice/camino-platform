spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/camino
    username: camino
    password: camino_local_pw
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080