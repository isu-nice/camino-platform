spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:camino}
    username: ${DB_USERNAME:camino}
    password: ${DB_PASSWORD:camino_local_pw}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080

jwt:
  secret: ${JWT_SECRET:camino-local-dev-secret-key-change-this-in-production-1234567890}